# 满分男 · 酒桌同屏局 — PRD v1.1

总 PM：Codex（续建）。发起人：Kim。日期：2026-07-25。

## 1. 一句话
一个公网可访问的多人酒桌 H5：同桌好友进同一房间，轮流"拷打"一人玩满分男/满分女游戏，用 0-10 打分猜分罚酒来测"谁最懂她"，若干轮后 AI 生成她的理想型立绘 + 网易云年报式分享海报（带二维码）。

## 2. 技术架构（免费白嫖栈）
- **部署**：Cloudflare Workers（免费版）+ Durable Objects（房间状态 + WebSocket 广播）+ Workers Static Assets（前端）。一个 worker 搞定前后端，正式公网 URL：`https://manfen-nan.kimnin-iup.workers.dev`。
- **前端**：无构建 vanilla JS + CSS 单页应用（`public/`），移动优先（390×844 为第一视口）。
- **实时**：WebSocket，每房间一个 Durable Object 实例，房间码 4 位。
- **立绘生成**：免费图像 API（Pollinations，无 key），服务端拼 prompt；样图必须经 PM 人审后才算通过。后续可换 image2（GPT 图像），接口层抽象成 `generateImage(prompt)`。
- **二维码**：客户端 JS 生成（内嵌轻量 qrcode 实现），指向站点 URL + 房间邀请。
- **无数据库**：房间状态活在 DO 内存 + DO storage，局终即焚。

## 3. 防同 ID 占号
- 入房昵称在房间内唯一，重名直接拒绝并提示改名。
- 首次入房下发 `playerToken`（存 localStorage）；断线/刷新凭 token 原座位回收，别人报同名无 token 不能顶号。
- 房主有踢人权（处理真僵尸位）。

## 4. 游戏流程
1. **建房**：房主设昵称 → 得 4 位房间码 + 邀请链接/二维码。可设每人轮数（默认 5，范围 3-8）、题库锅底。
2. **入房**：昵称 + 选个 emoji 酒杯头像。大厅显示全员，房主开局（≥2 人，建议 3-8 人）。
3. **选被拷打者**（游戏内称「今晚主角」）：抽签交互——**"抽酒签"**：屏幕出现一把竹酒签，摇晃后弹出一支，签上是主角名字（非转盘，有中式酒桌质感）。已当过主角的不再被抽。
4. **主角设定**：主角自选理想型性别：满分男 / 满分女 / 都行（都行则题目混发、称呼用 TA）。
5. **每轮**：
   - 发一道题：「这是一个满分男，但是他______。」
   - **主角**在自己手机上打分 0-10（10=无伤大雅仍然满分，0=一票否决直接火化）。
   - **其他人**同时盲猜主角的分数（0-10 滑条）。
   - 开牌动画：亮出主角分 + 各人猜的分。**|猜分 − 主角分| ≥ 3 → 罚酒**；精确命中 → 获「懂她 +1」并可指定一人喝。
   - 主角可点「补刀」发一句语音式吐槽文字（可选，显示在结果页）。
6. **换主角**：该主角轮数打完 → 回到抽酒签，换下一位，直到人人都当过主角（或房主提前收局）。
7. **Aha 结算**（每位主角轮次结束即生成，收局后可回看全部）：
   - 由该主角全部打分推断**理想型画像**：容忍了什么（≥7 分的缺陷）、绝不接受什么（≤2 分）、癖好倾向 → 拼 prompt 生成**理想型立绘**（插画风，性别按主角选择）。
   - 点击三段式揭晓：①乙游立绘亮相 → ②相亲档案卡（MBTI、生日、星座、职业身份、人设关键词）→ ③由打分数据驱动的相处细节（3-5 条，有梗、具体、像真的）。
   - **海报模式**：1080×1920 年报式长图（Canvas 合成）——立绘 + 人物档案 + 主角酒桌称号 + 关键数据（最懂你的人 / 一票否决 / 爆灯率）+ 底部站点二维码。一键保存图片转发。

## 5. 题库规范（PM 生成，两档锅底）
- 命名：轻量 =「清汤锅底」，重口 =「变态辣锅底」。
- 每档 ≥60 题，字段：`id, text, genderMode(male/female/neutral + 变体文案), tags, spice(1-5)`。
- 文风：短、口语、致命细节。**大量网络梗**：谐音黑话（SM →「赛百诺」「麦当劳」）、短剧梗人名（嘉欣/嘉豪式）、抽象话、地狱笑话边缘。屎尿屁性百无禁忌，但**不恶心、要好笑**——猎奇细节必须带幽默拐点，不写露骨描写、不写纯生理不适。
- 示例基调：
  - 清汤：「这是一个满分男，但他拿前任和朋友玩满分女游戏。」
  - 变态辣：「这是一个满分男，但他留着很长的小拇指指甲，说是用来开快递的，你没见他开过快递。」

## 6. 视觉方向
- 高对比冷白底 + 电光蓝大色块，近黑文字与粗边界保证酒桌弱光环境仍可读；冰晶/铬银层次提供高级感。
- 荧光黄只用于精确命中等正向高光，信号红只用于罚酒/灭灯等结果态；MBTI 穿搭色作为人物个性点缀，避免全站单一蓝色。
- 大字号、≥44px 触控目标，390×844 手机视口优先，同时适配 1400×900 桌面围观。
- 海报：同一套高对比视觉语言，纵向长图 1080×1920 逻辑尺寸，强数据排版并保留真实立绘与二维码。

## 7. QA 门槛（交付前必过）
- 双视口截图（390×844 优先 + 桌面）走 web-delivery-qa。
- 多端同房间端到端：建房→入房→重名拒绝→断线重连回座→完整一局→海报生成。
- 立绘样图 PM 人审 ≥3 张合格才接入。

## 9.5 理想型乙游化（Kim 追加，与 §9 同优先级）
- **刻板乙游长相体系**：立绘一眼可辨"是哪一挂"——参考市面大牌乙游（恋与深空/光与夜之恋/未定事件簿气质）建立原型库：霸总、小奶狗、小狼狗、疯批美人、禁欲系、阳光运动系、病娇文艺、姐系御姐、乖乖学霸等（男女/中性各配齐）。原型由主角打分数据映射（容忍/否决的 tags 聚类 → 原型 + MBTI）。
- **MBTI 绑定穿搭配色**：16 型各配一个签名配色方案（如 ENTJ 黑金、INFP 雾紫、ESFP 荧光橙…），写进生图 prompt 的服装颜色描述，档案卡上配色同步呈现。
- **三段式点击流**（替代原正反翻转）：① 立绘亮相（乙游卡面质感，此处接 §9 爆灯/灭灯）→ 点击 ② **相亲档案卡**：姓名（玩梗起名）、MBTI、出生年月日（由分数派生的彩蛋日期）、职业身份、身高、一句话人设，非诚勿扰嘉宾资料卡式排版 → 点击 ③ 相处细节（原有模板文案）。海报取档案卡+立绘合成。
- 生图 prompt 升级为：原型外貌关键词 + MBTI 配色服装 + 乙游卡面质感（细腻光效、高级时装感），样图仍需 PM 人审。

## 9. 非诚勿扰互动体系（Kim 追加，M3.5）
一套贯穿全程的"节目效果"互动层，aha 环节全面对标非诚勿扰现场：
- **爆灯/灭灯**：理想型立绘亮相时，除主角外人人有一盏灯，可「爆灯」（心动大特效：灯柱亮起+全屏光效）或「灭灯」（灯灭+哔声）；允许改票，以最后选择计入实时结果与海报数据（理想型获 X/Y 爆灯）。
- **弹幕**：全员可发弹幕横飘过屏幕（限长、限频防刷屏），aha 屏为主战场，答题/开牌屏同样开放。
- **聊天室**：侧边/底部抽屉式聊天室，全程可用；消息支持**飞书式贴 emoji 回应**（对任意消息贴表情，聚合计数显示，点过可取消）。
- **前置互动**：开牌等高潮节点提供快捷 reaction（如 🍺😅💔🔥）以弹幕/飘屏形式出现，互动语言全程一个体系。
- **音效/BGM**：不用有版权的原曲；WebAudio 合成"节目氛围"原创音效——爆灯璀璨音、灭灯哔声、理想型入场 riff/loop，可开关。若 Kim 提供已授权音频再替换。
- 协议新增消息：danmaku / chat / react(msgId,emoji) / light(on|off)，DO 内聊天记录限量滚动（如最近 100 条），局终即焚。

## 8. 里程碑
M1 PRD+题库 → M2 本地可玩完整局 → M3 高对比视觉+海报 → M3.5 全场互动+理想型三段卡 → M4 Cloudflare 公网部署 → M5 双视口 QA → M6 GitHub 分支+PR。
